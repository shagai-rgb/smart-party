{
  "name": "smart-party-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r preview"
  },
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "vite": "^6.4.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
